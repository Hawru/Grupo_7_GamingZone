<!doctype html>
<html lang="en">
  <%- include('../partials/head', {title: 'Gaming Zone - Log In'}) %>
  <body>
    <%- include('../partials/header') %>
    <main id="bg-profile" class="d-flex align-items-center p-4">
      <div class="container">
        <div class="row justify-content-center text-center">
          <div class="col-md-5 col-lg-4 rounded-4 p-4 shadow bg-dark" id="profile">
            <h2><%= profile.username %></h2>
            <div class="container">
                <img src="/images/<%=profile.profile_path%>" class="img-profile" alt="profile photo">
            </div>
            <h4><%= profile.email %> </h4>
            <a href="/users/logout">Cerrar Sesión</a>
          </div>
        </div>    
      </div>
    </main>
    <%- include('../partials/footer') %>
    <%- include('../partials/scripts') %>
  </body>
</html>